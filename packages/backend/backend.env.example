# ==================== 后端专用配置 ====================
# 此文件包含后端特有的配置项
# 主配置文件 .env 中的配置会自动继承

# ==================== 音频处理配置 ====================
# 音频文件存储目录
AUDIO_DIR=./audio
# 音频缓存目录
AUDIO_CACHE_DIR=./audio/.cache
# 公共文件目录
PUBLIC_DIR=./public

# ==================== 日志配置 ====================
# 日志级别 (error, warn, info, debug)
LOG_LEVEL=info
# 日志文件路径
LOG_FILE=./logs/app.log
# 错误日志文件路径
ERROR_LOG_FILE=./logs/error.log

# ==================== 开发配置 ====================
# 是否启用热重载
HOT_RELOAD=true
# 是否启用调试模式
DEBUG=false
# TypeScript 编译选项
TSC_WATCH=true

# ==================== FFmpeg 配置 ====================
# FFmpeg 可执行文件路径（自动检测，可手动指定）
FFMPEG_PATH=
# 音频质量设置
AUDIO_QUALITY=high
# 音频格式
AUDIO_FORMAT=mp3

# ==================== 缓存配置 ====================
# 缓存类型 (memory, file, redis)
CACHE_TYPE=file
# 缓存过期时间（毫秒）
CACHE_TTL=31536000000
# Redis 配置（如果使用 Redis 缓存）
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=
